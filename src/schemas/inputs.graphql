# mapping to inputsType.ts

input UserInput {
    username: String!,
    targets: [TargetInput!]
}

input TargetInput {
    name: String!,
    timeSpent: Int,
    timePieces: [TimePieceInput!]
}

input TimePieceInput {
    start: DateTime!,
    duration: Int!,
    type: String!
}
