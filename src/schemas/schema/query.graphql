type Query{
    """Test Connection"""
    test: Boolean!

    """Try to find one user"""
    user(username:String!):User

    """Try to find one target"""
    target(target_id:ID!):Target

    """Try to find timepieces for one user"""
    timepieces(user_id:ID!, first: Int!, after: String):TimePieceConnection!
}
